<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mpoznyak.casemanager">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.activity.WelcomeActivity">

        </activity>
        <activity android:name=".view.activity.WelcomeAddTypeActivity" />
        <activity android:name=".view.activity.NewTypeActivity" />
        <activity android:name=".view.activity.NewCaseActivity" />
        <activity android:name=".view.activity.CaseActivity" />
        <activity android:name=".view.activity.FileManagerActivity" />
        <activity android:name=".view.activity.MediaManagerActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>